/**
 * EduAI — User Controller  (Stage 2 scaffold)
 */

exports.getProfile   = async (_req, res, next) => { try { res.status(501).json({ message: 'Stage 2 — coming soon' }); } catch (e) { next(e); } };
exports.updateProfile= async (_req, res, next) => { try { res.status(501).json({ message: 'Stage 2 — coming soon' }); } catch (e) { next(e); } };
exports.getSaved     = async (_req, res, next) => { try { res.status(501).json({ message: 'Stage 2 — coming soon' }); } catch (e) { next(e); } };
exports.saveScheme   = async (_req, res, next) => { try { res.status(501).json({ message: 'Stage 2 — coming soon' }); } catch (e) { next(e); } };
exports.unsaveScheme = async (_req, res, next) => { try { res.status(501).json({ message: 'Stage 2 — coming soon' }); } catch (e) { next(e); } };
