# frontend/public/

Static assets that are served as-is (not processed).

## Contents (Stage 1)

- `favicon.ico` — Browser tab icon  
- `og-image.png` — Social media preview image (1200×630)

## Stage 2

When migrating to Vite, this folder becomes the standard Vite `public/` directory and its contents are copied to the build output root unchanged.
